# automatically generated by the FlatBuffers compiler, do not modify

# namespace: generated

import flatbuffers
from flatbuffers.compat import import_numpy
from typing import Any
from typing import Optional
np = import_numpy()

# Algorithm selection by registry name. Empty selects the default
# implementation of a stage.
class Algorithms(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset: int = 0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = Algorithms()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsAlgorithms(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # Algorithms
    def Init(self, buf: bytes, pos: int):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Algorithms
    def Assigner(self) -> Optional[str]:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Algorithms
    def GlobalRouter(self) -> Optional[str]:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Algorithms
    def DetailRouter(self) -> Optional[str]:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Algorithms
    def FinalRouter(self) -> Optional[str]:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

def AlgorithmsStart(builder: flatbuffers.Builder):
    builder.StartObject(4)

def Start(builder: flatbuffers.Builder):
    AlgorithmsStart(builder)

def AlgorithmsAddAssigner(builder: flatbuffers.Builder, assigner: int):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(assigner), 0)

def AddAssigner(builder: flatbuffers.Builder, assigner: int):
    AlgorithmsAddAssigner(builder, assigner)

def AlgorithmsAddGlobalRouter(builder: flatbuffers.Builder, globalRouter: int):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(globalRouter), 0)

def AddGlobalRouter(builder: flatbuffers.Builder, globalRouter: int):
    AlgorithmsAddGlobalRouter(builder, globalRouter)

def AlgorithmsAddDetailRouter(builder: flatbuffers.Builder, detailRouter: int):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(detailRouter), 0)

def AddDetailRouter(builder: flatbuffers.Builder, detailRouter: int):
    AlgorithmsAddDetailRouter(builder, detailRouter)

def AlgorithmsAddFinalRouter(builder: flatbuffers.Builder, finalRouter: int):
    builder.PrependUOffsetTRelativeSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(finalRouter), 0)

def AddFinalRouter(builder: flatbuffers.Builder, finalRouter: int):
    AlgorithmsAddFinalRouter(builder, finalRouter)

def AlgorithmsEnd(builder: flatbuffers.Builder) -> int:
    return builder.EndObject()

def End(builder: flatbuffers.Builder) -> int:
    return AlgorithmsEnd(builder)


class AlgorithmsT(object):

    # AlgorithmsT
    def __init__(
        self,
        assigner = None,
        globalRouter = None,
        detailRouter = None,
        finalRouter = None,
    ):
        self.assigner = assigner  # type: Optional[str]
        self.globalRouter = globalRouter  # type: Optional[str]
        self.detailRouter = detailRouter  # type: Optional[str]
        self.finalRouter = finalRouter  # type: Optional[str]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        algorithms = Algorithms()
        algorithms.Init(buf, pos)
        return cls.InitFromObj(algorithms)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, algorithms):
        x = AlgorithmsT()
        x._UnPack(algorithms)
        return x

    # AlgorithmsT
    def _UnPack(self, algorithms):
        if algorithms is None:
            return
        self.assigner = algorithms.Assigner()
        if self.assigner is not None:
            self.assigner = self.assigner.decode('utf-8')
        self.globalRouter = algorithms.GlobalRouter()
        if self.globalRouter is not None:
            self.globalRouter = self.globalRouter.decode('utf-8')
        self.detailRouter = algorithms.DetailRouter()
        if self.detailRouter is not None:
            self.detailRouter = self.detailRouter.decode('utf-8')
        self.finalRouter = algorithms.FinalRouter()
        if self.finalRouter is not None:
            self.finalRouter = self.finalRouter.decode('utf-8')

    # AlgorithmsT
    def Pack(self, builder):
        if self.assigner is not None:
            assigner = builder.CreateString(self.assigner)
        if self.globalRouter is not None:
            globalRouter = builder.CreateString(self.globalRouter)
        if self.detailRouter is not None:
            detailRouter = builder.CreateString(self.detailRouter)
        if self.finalRouter is not None:
            finalRouter = builder.CreateString(self.finalRouter)
        AlgorithmsStart(builder)
        if self.assigner is not None:
            AlgorithmsAddAssigner(builder, assigner)
        if self.globalRouter is not None:
            AlgorithmsAddGlobalRouter(builder, globalRouter)
        if self.detailRouter is not None:
            AlgorithmsAddDetailRouter(builder, detailRouter)
        if self.finalRouter is not None:
            AlgorithmsAddFinalRouter(builder, finalRouter)
        algorithms = AlgorithmsEnd(builder)
        return algorithms
