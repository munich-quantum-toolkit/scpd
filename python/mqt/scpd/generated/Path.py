# automatically generated by the FlatBuffers compiler, do not modify

# namespace: generated

import flatbuffers
from flatbuffers.compat import import_numpy
from typing import Any
from mqt.scpd.generated.Segment import Segment
from typing import Optional
np = import_numpy()

# A routed wire as a sequence of analytic segments, never as sampled points.
class Path(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset: int = 0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = Path()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsPath(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # Path
    def Init(self, buf: bytes, pos: int):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Path
    def Segments(self, j: int) -> Optional[Segment]:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            obj = Segment()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Path
    def SegmentsLength(self) -> int:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # Path
    def SegmentsIsNone(self) -> bool:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

def PathStart(builder: flatbuffers.Builder):
    builder.StartObject(1)

def Start(builder: flatbuffers.Builder):
    PathStart(builder)

def PathAddSegments(builder: flatbuffers.Builder, segments: int):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(segments), 0)

def AddSegments(builder: flatbuffers.Builder, segments: int):
    PathAddSegments(builder, segments)

def PathStartSegmentsVector(builder, numElems: int) -> int:
    return builder.StartVector(4, numElems, 4)

def StartSegmentsVector(builder, numElems: int) -> int:
    return PathStartSegmentsVector(builder, numElems)

def PathEnd(builder: flatbuffers.Builder) -> int:
    return builder.EndObject()

def End(builder: flatbuffers.Builder) -> int:
    return PathEnd(builder)

import mqt.scpd.generated.Segment
try:
    from typing import List
except:
    pass

class PathT(object):

    # PathT
    def __init__(
        self,
        segments = None,
    ):
        self.segments = segments  # type: Optional[List[mqt.scpd.generated.Segment.SegmentT]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        path = Path()
        path.Init(buf, pos)
        return cls.InitFromObj(path)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, path):
        x = PathT()
        x._UnPack(path)
        return x

    # PathT
    def _UnPack(self, path):
        if path is None:
            return
        if not path.SegmentsIsNone():
            self.segments = []
            for i in range(path.SegmentsLength()):
                if path.Segments(i) is None:
                    self.segments.append(None)
                else:
                    segment_ = mqt.scpd.generated.Segment.SegmentT.InitFromObj(path.Segments(i))
                    self.segments.append(segment_)

    # PathT
    def Pack(self, builder):
        if self.segments is not None:
            segmentslist = []
            for i in range(len(self.segments)):
                segmentslist.append(self.segments[i].Pack(builder))
            PathStartSegmentsVector(builder, len(self.segments))
            for i in reversed(range(len(self.segments))):
                builder.PrependUOffsetTRelative(segmentslist[i])
            segments = builder.EndVector()
        PathStart(builder)
        if self.segments is not None:
            PathAddSegments(builder, segments)
        path = PathEnd(builder)
        return path
