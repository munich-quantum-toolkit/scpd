# automatically generated by the FlatBuffers compiler, do not modify

# namespace: generated

import flatbuffers
from flatbuffers.compat import import_numpy
from typing import Any
np = import_numpy()

# The two ordered outer port sequences. Required under Manual, an error
# under Auto.
class PortSequences(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset: int = 0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = PortSequences()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsPortSequences(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # PortSequences
    def Init(self, buf: bytes, pos: int):
        self._tab = flatbuffers.table.Table(buf, pos)

    # PortSequences
    def AllOuter(self, j: int):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.String(a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return ""

    # PortSequences
    def AllOuterLength(self) -> int:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # PortSequences
    def AllOuterIsNone(self) -> bool:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

    # PortSequences
    def FixedOuter(self, j: int):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.String(a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return ""

    # PortSequences
    def FixedOuterLength(self) -> int:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # PortSequences
    def FixedOuterIsNone(self) -> bool:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

def PortSequencesStart(builder: flatbuffers.Builder):
    builder.StartObject(2)

def Start(builder: flatbuffers.Builder):
    PortSequencesStart(builder)

def PortSequencesAddAllOuter(builder: flatbuffers.Builder, allOuter: int):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(allOuter), 0)

def AddAllOuter(builder: flatbuffers.Builder, allOuter: int):
    PortSequencesAddAllOuter(builder, allOuter)

def PortSequencesStartAllOuterVector(builder, numElems: int) -> int:
    return builder.StartVector(4, numElems, 4)

def StartAllOuterVector(builder, numElems: int) -> int:
    return PortSequencesStartAllOuterVector(builder, numElems)

def PortSequencesAddFixedOuter(builder: flatbuffers.Builder, fixedOuter: int):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(fixedOuter), 0)

def AddFixedOuter(builder: flatbuffers.Builder, fixedOuter: int):
    PortSequencesAddFixedOuter(builder, fixedOuter)

def PortSequencesStartFixedOuterVector(builder, numElems: int) -> int:
    return builder.StartVector(4, numElems, 4)

def StartFixedOuterVector(builder, numElems: int) -> int:
    return PortSequencesStartFixedOuterVector(builder, numElems)

def PortSequencesEnd(builder: flatbuffers.Builder) -> int:
    return builder.EndObject()

def End(builder: flatbuffers.Builder) -> int:
    return PortSequencesEnd(builder)

try:
    from typing import List
except:
    pass

class PortSequencesT(object):

    # PortSequencesT
    def __init__(
        self,
        allOuter = None,
        fixedOuter = None,
    ):
        self.allOuter = allOuter  # type: Optional[List[Optional[str]]]
        self.fixedOuter = fixedOuter  # type: Optional[List[Optional[str]]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        portSequences = PortSequences()
        portSequences.Init(buf, pos)
        return cls.InitFromObj(portSequences)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, portSequences):
        x = PortSequencesT()
        x._UnPack(portSequences)
        return x

    # PortSequencesT
    def _UnPack(self, portSequences):
        if portSequences is None:
            return
        if not portSequences.AllOuterIsNone():
            self.allOuter = []
            for i in range(portSequences.AllOuterLength()):
                self.allOuter.append(portSequences.AllOuter(i))
        if not portSequences.FixedOuterIsNone():
            self.fixedOuter = []
            for i in range(portSequences.FixedOuterLength()):
                self.fixedOuter.append(portSequences.FixedOuter(i))

    # PortSequencesT
    def Pack(self, builder):
        if self.allOuter is not None:
            allOuterlist = []
            for i in range(len(self.allOuter)):
                allOuterlist.append(builder.CreateString(self.allOuter[i]))
            PortSequencesStartAllOuterVector(builder, len(self.allOuter))
            for i in reversed(range(len(self.allOuter))):
                builder.PrependUOffsetTRelative(allOuterlist[i])
            allOuter = builder.EndVector()
        if self.fixedOuter is not None:
            fixedOuterlist = []
            for i in range(len(self.fixedOuter)):
                fixedOuterlist.append(builder.CreateString(self.fixedOuter[i]))
            PortSequencesStartFixedOuterVector(builder, len(self.fixedOuter))
            for i in reversed(range(len(self.fixedOuter))):
                builder.PrependUOffsetTRelative(fixedOuterlist[i])
            fixedOuter = builder.EndVector()
        PortSequencesStart(builder)
        if self.allOuter is not None:
            PortSequencesAddAllOuter(builder, allOuter)
        if self.fixedOuter is not None:
            PortSequencesAddFixedOuter(builder, fixedOuter)
        portSequences = PortSequencesEnd(builder)
        return portSequences
