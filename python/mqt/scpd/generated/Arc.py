# automatically generated by the FlatBuffers compiler, do not modify

# namespace: generated

import flatbuffers
from flatbuffers.compat import import_numpy
from typing import Any
from mqt.scpd.generated.Point import Point
from typing import Optional
np = import_numpy()

# A circular arc in layout space with an exact centre and radius. Angles are
# radians; a positive sweep runs counter-clockwise.
class Arc(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset: int = 0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = Arc()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsArc(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # Arc
    def Init(self, buf: bytes, pos: int):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Arc
    def Centre(self) -> Optional[Point]:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = o + self._tab.Pos
            obj = Point()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Arc
    def Radius(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Arc
    def StartAngle(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

    # Arc
    def Sweep(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

def ArcStart(builder: flatbuffers.Builder):
    builder.StartObject(4)

def Start(builder: flatbuffers.Builder):
    ArcStart(builder)

def ArcAddCentre(builder: flatbuffers.Builder, centre: Any):
    builder.PrependStructSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(centre), 0)

def AddCentre(builder: flatbuffers.Builder, centre: Any):
    ArcAddCentre(builder, centre)

def ArcAddRadius(builder: flatbuffers.Builder, radius: float):
    builder.PrependFloat64Slot(1, radius, 0.0)

def AddRadius(builder: flatbuffers.Builder, radius: float):
    ArcAddRadius(builder, radius)

def ArcAddStartAngle(builder: flatbuffers.Builder, startAngle: float):
    builder.PrependFloat64Slot(2, startAngle, 0.0)

def AddStartAngle(builder: flatbuffers.Builder, startAngle: float):
    ArcAddStartAngle(builder, startAngle)

def ArcAddSweep(builder: flatbuffers.Builder, sweep: float):
    builder.PrependFloat64Slot(3, sweep, 0.0)

def AddSweep(builder: flatbuffers.Builder, sweep: float):
    ArcAddSweep(builder, sweep)

def ArcEnd(builder: flatbuffers.Builder) -> int:
    return builder.EndObject()

def End(builder: flatbuffers.Builder) -> int:
    return ArcEnd(builder)

import mqt.scpd.generated.Point
try:
    from typing import Optional
except:
    pass

class ArcT(object):

    # ArcT
    def __init__(
        self,
        centre = None,
        radius = 0.0,
        startAngle = 0.0,
        sweep = 0.0,
    ):
        self.centre = centre  # type: Optional[mqt.scpd.generated.Point.PointT]
        self.radius = radius  # type: float
        self.startAngle = startAngle  # type: float
        self.sweep = sweep  # type: float

    @classmethod
    def InitFromBuf(cls, buf, pos):
        arc = Arc()
        arc.Init(buf, pos)
        return cls.InitFromObj(arc)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, arc):
        x = ArcT()
        x._UnPack(arc)
        return x

    # ArcT
    def _UnPack(self, arc):
        if arc is None:
            return
        if arc.Centre() is not None:
            self.centre = mqt.scpd.generated.Point.PointT.InitFromObj(arc.Centre())
        self.radius = arc.Radius()
        self.startAngle = arc.StartAngle()
        self.sweep = arc.Sweep()

    # ArcT
    def Pack(self, builder):
        ArcStart(builder)
        if self.centre is not None:
            centre = self.centre.Pack(builder)
            ArcAddCentre(builder, centre)
        ArcAddRadius(builder, self.radius)
        ArcAddStartAngle(builder, self.startAngle)
        ArcAddSweep(builder, self.sweep)
        arc = ArcEnd(builder)
        return arc
