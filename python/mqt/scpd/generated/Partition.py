# automatically generated by the FlatBuffers compiler, do not modify

# namespace: generated

import flatbuffers
from flatbuffers.compat import import_numpy
from typing import Any
from mqt.scpd.generated.GCoord import GCoord
from typing import Optional
np = import_numpy()

# A region of free space on the coarse grid, produced by the watershed.
class Partition(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset: int = 0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = Partition()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsPartition(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def PartitionBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x53\x43\x50\x31", size_prefixed=size_prefixed)

    # Partition
    def Init(self, buf: bytes, pos: int):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Partition
    def Id(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # Partition
    def Cells(self, j: int) -> Optional[GCoord]:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 8
            obj = GCoord()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Partition
    def CellsLength(self) -> int:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # Partition
    def CellsIsNone(self) -> bool:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

def PartitionStart(builder: flatbuffers.Builder):
    builder.StartObject(2)

def Start(builder: flatbuffers.Builder):
    PartitionStart(builder)

def PartitionAddId(builder: flatbuffers.Builder, id: int):
    builder.PrependUint32Slot(0, id, 0)

def AddId(builder: flatbuffers.Builder, id: int):
    PartitionAddId(builder, id)

def PartitionAddCells(builder: flatbuffers.Builder, cells: int):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(cells), 0)

def AddCells(builder: flatbuffers.Builder, cells: int):
    PartitionAddCells(builder, cells)

def PartitionStartCellsVector(builder, numElems: int) -> int:
    return builder.StartVector(8, numElems, 4)

def StartCellsVector(builder, numElems: int) -> int:
    return PartitionStartCellsVector(builder, numElems)

def PartitionEnd(builder: flatbuffers.Builder) -> int:
    return builder.EndObject()

def End(builder: flatbuffers.Builder) -> int:
    return PartitionEnd(builder)

import mqt.scpd.generated.GCoord
try:
    from typing import List
except:
    pass

class PartitionT(object):

    # PartitionT
    def __init__(
        self,
        id = 0,
        cells = None,
    ):
        self.id = id  # type: int
        self.cells = cells  # type: Optional[List[mqt.scpd.generated.GCoord.GCoordT]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        partition = Partition()
        partition.Init(buf, pos)
        return cls.InitFromObj(partition)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, partition):
        x = PartitionT()
        x._UnPack(partition)
        return x

    # PartitionT
    def _UnPack(self, partition):
        if partition is None:
            return
        self.id = partition.Id()
        if not partition.CellsIsNone():
            self.cells = []
            for i in range(partition.CellsLength()):
                if partition.Cells(i) is None:
                    self.cells.append(None)
                else:
                    gCoord_ = mqt.scpd.generated.GCoord.GCoordT.InitFromObj(partition.Cells(i))
                    self.cells.append(gCoord_)

    # PartitionT
    def Pack(self, builder):
        if self.cells is not None:
            PartitionStartCellsVector(builder, len(self.cells))
            for i in reversed(range(len(self.cells))):
                self.cells[i].Pack(builder)
            cells = builder.EndVector()
        PartitionStart(builder)
        PartitionAddId(builder, self.id)
        if self.cells is not None:
            PartitionAddCells(builder, cells)
        partition = PartitionEnd(builder)
        return partition
