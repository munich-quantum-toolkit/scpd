# automatically generated by the FlatBuffers compiler, do not modify

# namespace: generated

import flatbuffers
from flatbuffers.compat import import_numpy
from typing import Any
from mqt.scpd.generated.Bridge import Bridge
from mqt.scpd.generated.CpwCoupler import CpwCoupler
from mqt.scpd.generated.Wire import Wire
from typing import Optional
np = import_numpy()

# Output of the Finalize stage: the geometry IR.
class Geometry(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset: int = 0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = Geometry()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsGeometry(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def GeometryBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x53\x43\x50\x31", size_prefixed=size_prefixed)

    # Geometry
    def Init(self, buf: bytes, pos: int):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Geometry
    def Wires(self, j: int) -> Optional[Wire]:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            obj = Wire()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Geometry
    def WiresLength(self) -> int:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # Geometry
    def WiresIsNone(self) -> bool:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

    # Geometry
    def Couplers(self, j: int) -> Optional[CpwCoupler]:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            obj = CpwCoupler()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Geometry
    def CouplersLength(self) -> int:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # Geometry
    def CouplersIsNone(self) -> bool:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

    # Geometry
    def Bridges(self, j: int) -> Optional[Bridge]:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            obj = Bridge()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Geometry
    def BridgesLength(self) -> int:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # Geometry
    def BridgesIsNone(self) -> bool:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        return o == 0

def GeometryStart(builder: flatbuffers.Builder):
    builder.StartObject(3)

def Start(builder: flatbuffers.Builder):
    GeometryStart(builder)

def GeometryAddWires(builder: flatbuffers.Builder, wires: int):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(wires), 0)

def AddWires(builder: flatbuffers.Builder, wires: int):
    GeometryAddWires(builder, wires)

def GeometryStartWiresVector(builder, numElems: int) -> int:
    return builder.StartVector(4, numElems, 4)

def StartWiresVector(builder, numElems: int) -> int:
    return GeometryStartWiresVector(builder, numElems)

def GeometryAddCouplers(builder: flatbuffers.Builder, couplers: int):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(couplers), 0)

def AddCouplers(builder: flatbuffers.Builder, couplers: int):
    GeometryAddCouplers(builder, couplers)

def GeometryStartCouplersVector(builder, numElems: int) -> int:
    return builder.StartVector(4, numElems, 4)

def StartCouplersVector(builder, numElems: int) -> int:
    return GeometryStartCouplersVector(builder, numElems)

def GeometryAddBridges(builder: flatbuffers.Builder, bridges: int):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(bridges), 0)

def AddBridges(builder: flatbuffers.Builder, bridges: int):
    GeometryAddBridges(builder, bridges)

def GeometryStartBridgesVector(builder, numElems: int) -> int:
    return builder.StartVector(4, numElems, 4)

def StartBridgesVector(builder, numElems: int) -> int:
    return GeometryStartBridgesVector(builder, numElems)

def GeometryEnd(builder: flatbuffers.Builder) -> int:
    return builder.EndObject()

def End(builder: flatbuffers.Builder) -> int:
    return GeometryEnd(builder)

import mqt.scpd.generated.Bridge
import mqt.scpd.generated.CpwCoupler
import mqt.scpd.generated.Wire
try:
    from typing import List
except:
    pass

class GeometryT(object):

    # GeometryT
    def __init__(
        self,
        wires = None,
        couplers = None,
        bridges = None,
    ):
        self.wires = wires  # type: Optional[List[mqt.scpd.generated.Wire.WireT]]
        self.couplers = couplers  # type: Optional[List[mqt.scpd.generated.CpwCoupler.CpwCouplerT]]
        self.bridges = bridges  # type: Optional[List[mqt.scpd.generated.Bridge.BridgeT]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        geometry = Geometry()
        geometry.Init(buf, pos)
        return cls.InitFromObj(geometry)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, geometry):
        x = GeometryT()
        x._UnPack(geometry)
        return x

    # GeometryT
    def _UnPack(self, geometry):
        if geometry is None:
            return
        if not geometry.WiresIsNone():
            self.wires = []
            for i in range(geometry.WiresLength()):
                if geometry.Wires(i) is None:
                    self.wires.append(None)
                else:
                    wire_ = mqt.scpd.generated.Wire.WireT.InitFromObj(geometry.Wires(i))
                    self.wires.append(wire_)
        if not geometry.CouplersIsNone():
            self.couplers = []
            for i in range(geometry.CouplersLength()):
                if geometry.Couplers(i) is None:
                    self.couplers.append(None)
                else:
                    cpwCoupler_ = mqt.scpd.generated.CpwCoupler.CpwCouplerT.InitFromObj(geometry.Couplers(i))
                    self.couplers.append(cpwCoupler_)
        if not geometry.BridgesIsNone():
            self.bridges = []
            for i in range(geometry.BridgesLength()):
                if geometry.Bridges(i) is None:
                    self.bridges.append(None)
                else:
                    bridge_ = mqt.scpd.generated.Bridge.BridgeT.InitFromObj(geometry.Bridges(i))
                    self.bridges.append(bridge_)

    # GeometryT
    def Pack(self, builder):
        if self.wires is not None:
            wireslist = []
            for i in range(len(self.wires)):
                wireslist.append(self.wires[i].Pack(builder))
            GeometryStartWiresVector(builder, len(self.wires))
            for i in reversed(range(len(self.wires))):
                builder.PrependUOffsetTRelative(wireslist[i])
            wires = builder.EndVector()
        if self.couplers is not None:
            couplerslist = []
            for i in range(len(self.couplers)):
                couplerslist.append(self.couplers[i].Pack(builder))
            GeometryStartCouplersVector(builder, len(self.couplers))
            for i in reversed(range(len(self.couplers))):
                builder.PrependUOffsetTRelative(couplerslist[i])
            couplers = builder.EndVector()
        if self.bridges is not None:
            bridgeslist = []
            for i in range(len(self.bridges)):
                bridgeslist.append(self.bridges[i].Pack(builder))
            GeometryStartBridgesVector(builder, len(self.bridges))
            for i in reversed(range(len(self.bridges))):
                builder.PrependUOffsetTRelative(bridgeslist[i])
            bridges = builder.EndVector()
        GeometryStart(builder)
        if self.wires is not None:
            GeometryAddWires(builder, wires)
        if self.couplers is not None:
            GeometryAddCouplers(builder, couplers)
        if self.bridges is not None:
            GeometryAddBridges(builder, bridges)
        geometry = GeometryEnd(builder)
        return geometry
