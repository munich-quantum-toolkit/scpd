# automatically generated by the FlatBuffers compiler, do not modify

# namespace: generated

import flatbuffers
from flatbuffers.compat import import_numpy
from typing import Any
from mqt.scpd.generated.Connection import Connection
from typing import Optional
np = import_numpy()

# Output of the Assignment stage.
class Assignment(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset: int = 0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = Assignment()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsAssignment(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def AssignmentBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x53\x43\x50\x31", size_prefixed=size_prefixed)

    # Assignment
    def Init(self, buf: bytes, pos: int):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Assignment
    def Connections(self, j: int) -> Optional[Connection]:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            x = self._tab.Indirect(x)
            obj = Connection()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Assignment
    def ConnectionsLength(self) -> int:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # Assignment
    def ConnectionsIsNone(self) -> bool:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

    # Assignment
    def Objective(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Float64Flags, o + self._tab.Pos)
        return 0.0

def AssignmentStart(builder: flatbuffers.Builder):
    builder.StartObject(2)

def Start(builder: flatbuffers.Builder):
    AssignmentStart(builder)

def AssignmentAddConnections(builder: flatbuffers.Builder, connections: int):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(connections), 0)

def AddConnections(builder: flatbuffers.Builder, connections: int):
    AssignmentAddConnections(builder, connections)

def AssignmentStartConnectionsVector(builder, numElems: int) -> int:
    return builder.StartVector(4, numElems, 4)

def StartConnectionsVector(builder, numElems: int) -> int:
    return AssignmentStartConnectionsVector(builder, numElems)

def AssignmentAddObjective(builder: flatbuffers.Builder, objective: float):
    builder.PrependFloat64Slot(1, objective, 0.0)

def AddObjective(builder: flatbuffers.Builder, objective: float):
    AssignmentAddObjective(builder, objective)

def AssignmentEnd(builder: flatbuffers.Builder) -> int:
    return builder.EndObject()

def End(builder: flatbuffers.Builder) -> int:
    return AssignmentEnd(builder)

import mqt.scpd.generated.Connection
try:
    from typing import List
except:
    pass

class AssignmentT(object):

    # AssignmentT
    def __init__(
        self,
        connections = None,
        objective = 0.0,
    ):
        self.connections = connections  # type: Optional[List[mqt.scpd.generated.Connection.ConnectionT]]
        self.objective = objective  # type: float

    @classmethod
    def InitFromBuf(cls, buf, pos):
        assignment = Assignment()
        assignment.Init(buf, pos)
        return cls.InitFromObj(assignment)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, assignment):
        x = AssignmentT()
        x._UnPack(assignment)
        return x

    # AssignmentT
    def _UnPack(self, assignment):
        if assignment is None:
            return
        if not assignment.ConnectionsIsNone():
            self.connections = []
            for i in range(assignment.ConnectionsLength()):
                if assignment.Connections(i) is None:
                    self.connections.append(None)
                else:
                    connection_ = mqt.scpd.generated.Connection.ConnectionT.InitFromObj(assignment.Connections(i))
                    self.connections.append(connection_)
        self.objective = assignment.Objective()

    # AssignmentT
    def Pack(self, builder):
        if self.connections is not None:
            connectionslist = []
            for i in range(len(self.connections)):
                connectionslist.append(self.connections[i].Pack(builder))
            AssignmentStartConnectionsVector(builder, len(self.connections))
            for i in reversed(range(len(self.connections))):
                builder.PrependUOffsetTRelative(connectionslist[i])
            connections = builder.EndVector()
        AssignmentStart(builder)
        if self.connections is not None:
            AssignmentAddConnections(builder, connections)
        AssignmentAddObjective(builder, self.objective)
        assignment = AssignmentEnd(builder)
        return assignment
