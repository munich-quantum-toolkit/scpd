# automatically generated by the FlatBuffers compiler, do not modify

# namespace: generated

import flatbuffers
from flatbuffers.compat import import_numpy
from typing import Any
from mqt.scpd.generated.PortRef import PortRef
from typing import Optional
np = import_numpy()

# A corridor of partitions that a group of wires routes along.
class CapacityChain(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset: int = 0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = CapacityChain()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsCapacityChain(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def CapacityChainBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x53\x43\x50\x31", size_prefixed=size_prefixed)

    # CapacityChain
    def Init(self, buf: bytes, pos: int):
        self._tab = flatbuffers.table.Table(buf, pos)

    # CapacityChain
    def Ports(self, j: int) -> Optional[PortRef]:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 4
            obj = PortRef()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # CapacityChain
    def PortsLength(self) -> int:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # CapacityChain
    def PortsIsNone(self) -> bool:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

    # Partition ids, in routing order.
    # CapacityChain
    def Partitions(self, j: int):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return 0

    # CapacityChain
    def PartitionsAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint32Flags, o)
        return 0

    # CapacityChain
    def PartitionsLength(self) -> int:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # CapacityChain
    def PartitionsIsNone(self) -> bool:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

def CapacityChainStart(builder: flatbuffers.Builder):
    builder.StartObject(2)

def Start(builder: flatbuffers.Builder):
    CapacityChainStart(builder)

def CapacityChainAddPorts(builder: flatbuffers.Builder, ports: int):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(ports), 0)

def AddPorts(builder: flatbuffers.Builder, ports: int):
    CapacityChainAddPorts(builder, ports)

def CapacityChainStartPortsVector(builder, numElems: int) -> int:
    return builder.StartVector(4, numElems, 4)

def StartPortsVector(builder, numElems: int) -> int:
    return CapacityChainStartPortsVector(builder, numElems)

def CapacityChainAddPartitions(builder: flatbuffers.Builder, partitions: int):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(partitions), 0)

def AddPartitions(builder: flatbuffers.Builder, partitions: int):
    CapacityChainAddPartitions(builder, partitions)

def CapacityChainStartPartitionsVector(builder, numElems: int) -> int:
    return builder.StartVector(4, numElems, 4)

def StartPartitionsVector(builder, numElems: int) -> int:
    return CapacityChainStartPartitionsVector(builder, numElems)

def CapacityChainEnd(builder: flatbuffers.Builder) -> int:
    return builder.EndObject()

def End(builder: flatbuffers.Builder) -> int:
    return CapacityChainEnd(builder)

import mqt.scpd.generated.PortRef
try:
    from typing import List
except:
    pass

class CapacityChainT(object):

    # CapacityChainT
    def __init__(
        self,
        ports = None,
        partitions = None,
    ):
        self.ports = ports  # type: Optional[List[mqt.scpd.generated.PortRef.PortRefT]]
        self.partitions = partitions  # type: Optional[List[int]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        capacityChain = CapacityChain()
        capacityChain.Init(buf, pos)
        return cls.InitFromObj(capacityChain)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, capacityChain):
        x = CapacityChainT()
        x._UnPack(capacityChain)
        return x

    # CapacityChainT
    def _UnPack(self, capacityChain):
        if capacityChain is None:
            return
        if not capacityChain.PortsIsNone():
            self.ports = []
            for i in range(capacityChain.PortsLength()):
                if capacityChain.Ports(i) is None:
                    self.ports.append(None)
                else:
                    portRef_ = mqt.scpd.generated.PortRef.PortRefT.InitFromObj(capacityChain.Ports(i))
                    self.ports.append(portRef_)
        if not capacityChain.PartitionsIsNone():
            if np is None:
                self.partitions = []
                for i in range(capacityChain.PartitionsLength()):
                    self.partitions.append(capacityChain.Partitions(i))
            else:
                self.partitions = capacityChain.PartitionsAsNumpy()

    # CapacityChainT
    def Pack(self, builder):
        if self.ports is not None:
            CapacityChainStartPortsVector(builder, len(self.ports))
            for i in reversed(range(len(self.ports))):
                self.ports[i].Pack(builder)
            ports = builder.EndVector()
        if self.partitions is not None:
            if np is not None and type(self.partitions) is np.ndarray:
                partitions = builder.CreateNumpyVector(self.partitions)
            else:
                CapacityChainStartPartitionsVector(builder, len(self.partitions))
                for i in reversed(range(len(self.partitions))):
                    builder.PrependUint32(self.partitions[i])
                partitions = builder.EndVector()
        CapacityChainStart(builder)
        if self.ports is not None:
            CapacityChainAddPorts(builder, ports)
        if self.partitions is not None:
            CapacityChainAddPartitions(builder, partitions)
        capacityChain = CapacityChainEnd(builder)
        return capacityChain
