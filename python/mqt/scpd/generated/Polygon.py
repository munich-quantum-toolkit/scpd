# automatically generated by the FlatBuffers compiler, do not modify

# namespace: generated

import flatbuffers
from flatbuffers.compat import import_numpy
from typing import Any
from mqt.scpd.generated.Point import Point
from typing import Optional
np = import_numpy()

# A closed polygon in layout space, stored as the input carries it.
class Polygon(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset: int = 0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = Polygon()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsPolygon(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # Polygon
    def Init(self, buf: bytes, pos: int):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Polygon
    def Vertices(self, j: int) -> Optional[Point]:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 16
            obj = Point()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Polygon
    def VerticesLength(self) -> int:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # Polygon
    def VerticesIsNone(self) -> bool:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

def PolygonStart(builder: flatbuffers.Builder):
    builder.StartObject(1)

def Start(builder: flatbuffers.Builder):
    PolygonStart(builder)

def PolygonAddVertices(builder: flatbuffers.Builder, vertices: int):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(vertices), 0)

def AddVertices(builder: flatbuffers.Builder, vertices: int):
    PolygonAddVertices(builder, vertices)

def PolygonStartVerticesVector(builder, numElems: int) -> int:
    return builder.StartVector(16, numElems, 8)

def StartVerticesVector(builder, numElems: int) -> int:
    return PolygonStartVerticesVector(builder, numElems)

def PolygonEnd(builder: flatbuffers.Builder) -> int:
    return builder.EndObject()

def End(builder: flatbuffers.Builder) -> int:
    return PolygonEnd(builder)

import mqt.scpd.generated.Point
try:
    from typing import List
except:
    pass

class PolygonT(object):

    # PolygonT
    def __init__(
        self,
        vertices = None,
    ):
        self.vertices = vertices  # type: Optional[List[mqt.scpd.generated.Point.PointT]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        polygon = Polygon()
        polygon.Init(buf, pos)
        return cls.InitFromObj(polygon)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, polygon):
        x = PolygonT()
        x._UnPack(polygon)
        return x

    # PolygonT
    def _UnPack(self, polygon):
        if polygon is None:
            return
        if not polygon.VerticesIsNone():
            self.vertices = []
            for i in range(polygon.VerticesLength()):
                if polygon.Vertices(i) is None:
                    self.vertices.append(None)
                else:
                    point_ = mqt.scpd.generated.Point.PointT.InitFromObj(polygon.Vertices(i))
                    self.vertices.append(point_)

    # PolygonT
    def Pack(self, builder):
        if self.vertices is not None:
            PolygonStartVerticesVector(builder, len(self.vertices))
            for i in reversed(range(len(self.vertices))):
                self.vertices[i].Pack(builder)
            vertices = builder.EndVector()
        PolygonStart(builder)
        if self.vertices is not None:
            PolygonAddVertices(builder, vertices)
        polygon = PolygonEnd(builder)
        return polygon
