# automatically generated by the FlatBuffers compiler, do not modify

# namespace: generated

import flatbuffers
from flatbuffers.compat import import_numpy
from typing import Any
from mqt.scpd.generated.DCoord import DCoord
from typing import Optional
np = import_numpy()

# One connection's path on the detail grid.
class PixelPath(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset: int = 0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = PixelPath()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsPixelPath(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def PixelPathBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x53\x43\x50\x31", size_prefixed=size_prefixed)

    # PixelPath
    def Init(self, buf: bytes, pos: int):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Index into Assignment.connections.
    # PixelPath
    def Connection(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # PixelPath
    def Pixels(self, j: int) -> Optional[DCoord]:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 8
            obj = DCoord()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # PixelPath
    def PixelsLength(self) -> int:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # PixelPath
    def PixelsIsNone(self) -> bool:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        return o == 0

def PixelPathStart(builder: flatbuffers.Builder):
    builder.StartObject(2)

def Start(builder: flatbuffers.Builder):
    PixelPathStart(builder)

def PixelPathAddConnection(builder: flatbuffers.Builder, connection: int):
    builder.PrependUint32Slot(0, connection, 0)

def AddConnection(builder: flatbuffers.Builder, connection: int):
    PixelPathAddConnection(builder, connection)

def PixelPathAddPixels(builder: flatbuffers.Builder, pixels: int):
    builder.PrependUOffsetTRelativeSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(pixels), 0)

def AddPixels(builder: flatbuffers.Builder, pixels: int):
    PixelPathAddPixels(builder, pixels)

def PixelPathStartPixelsVector(builder, numElems: int) -> int:
    return builder.StartVector(8, numElems, 4)

def StartPixelsVector(builder, numElems: int) -> int:
    return PixelPathStartPixelsVector(builder, numElems)

def PixelPathEnd(builder: flatbuffers.Builder) -> int:
    return builder.EndObject()

def End(builder: flatbuffers.Builder) -> int:
    return PixelPathEnd(builder)

import mqt.scpd.generated.DCoord
try:
    from typing import List
except:
    pass

class PixelPathT(object):

    # PixelPathT
    def __init__(
        self,
        connection = 0,
        pixels = None,
    ):
        self.connection = connection  # type: int
        self.pixels = pixels  # type: Optional[List[mqt.scpd.generated.DCoord.DCoordT]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        pixelPath = PixelPath()
        pixelPath.Init(buf, pos)
        return cls.InitFromObj(pixelPath)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, pixelPath):
        x = PixelPathT()
        x._UnPack(pixelPath)
        return x

    # PixelPathT
    def _UnPack(self, pixelPath):
        if pixelPath is None:
            return
        self.connection = pixelPath.Connection()
        if not pixelPath.PixelsIsNone():
            self.pixels = []
            for i in range(pixelPath.PixelsLength()):
                if pixelPath.Pixels(i) is None:
                    self.pixels.append(None)
                else:
                    dCoord_ = mqt.scpd.generated.DCoord.DCoordT.InitFromObj(pixelPath.Pixels(i))
                    self.pixels.append(dCoord_)

    # PixelPathT
    def Pack(self, builder):
        if self.pixels is not None:
            PixelPathStartPixelsVector(builder, len(self.pixels))
            for i in reversed(range(len(self.pixels))):
                self.pixels[i].Pack(builder)
            pixels = builder.EndVector()
        PixelPathStart(builder)
        PixelPathAddConnection(builder, self.connection)
        if self.pixels is not None:
            PixelPathAddPixels(builder, pixels)
        pixelPath = PixelPathEnd(builder)
        return pixelPath
