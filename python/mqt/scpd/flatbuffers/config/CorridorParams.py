# automatically generated by the FlatBuffers compiler, do not modify

# namespace: config

import flatbuffers
from flatbuffers.compat import import_numpy
from typing import Any
from typing import Optional
np = import_numpy()

# What the Corridor stage is allowed to do.
class CorridorParams(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset: int = 0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = CorridorParams()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsCorridorParams(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # CorridorParams
    def Init(self, buf: bytes, pos: int):
        self._tab = flatbuffers.table.Table(buf, pos)

    # The implementation, by the name it is registered under.
    # CorridorParams
    def Router(self) -> Optional[str]:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.String(o + self._tab.Pos)
        return None

    # Sweeps over the connection list, alternating forward and backward.
    #
    # A wire that routes stays where it is; the sweeps exist so that a wire
    # which found no way through can take the room a later one has not
    # claimed yet, and so that the order the connections are tried in does
    # not decide the result on its own.
    # CorridorParams
    def Rounds(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 12

    # How many neighbouring connections a failed one may rip up, counted
    # along the sweep direction.
    # CorridorParams
    def MaxRelaxation(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 30

def CorridorParamsStart(builder: flatbuffers.Builder):
    builder.StartObject(3)

def Start(builder: flatbuffers.Builder):
    CorridorParamsStart(builder)

def CorridorParamsAddRouter(builder: flatbuffers.Builder, router: int):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(router), 0)

def AddRouter(builder: flatbuffers.Builder, router: int):
    CorridorParamsAddRouter(builder, router)

def CorridorParamsAddRounds(builder: flatbuffers.Builder, rounds: int):
    builder.PrependUint32Slot(1, rounds, 12)

def AddRounds(builder: flatbuffers.Builder, rounds: int):
    CorridorParamsAddRounds(builder, rounds)

def CorridorParamsAddMaxRelaxation(builder: flatbuffers.Builder, maxRelaxation: int):
    builder.PrependUint32Slot(2, maxRelaxation, 30)

def AddMaxRelaxation(builder: flatbuffers.Builder, maxRelaxation: int):
    CorridorParamsAddMaxRelaxation(builder, maxRelaxation)

def CorridorParamsEnd(builder: flatbuffers.Builder) -> int:
    return builder.EndObject()

def End(builder: flatbuffers.Builder) -> int:
    return CorridorParamsEnd(builder)


class CorridorParamsT(object):

    # CorridorParamsT
    def __init__(
        self,
        router = None,
        rounds = 12,
        maxRelaxation = 30,
    ):
        self.router = router  # type: Optional[str]
        self.rounds = rounds  # type: int
        self.maxRelaxation = maxRelaxation  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        corridorParams = CorridorParams()
        corridorParams.Init(buf, pos)
        return cls.InitFromObj(corridorParams)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, corridorParams):
        x = CorridorParamsT()
        x._UnPack(corridorParams)
        return x

    # CorridorParamsT
    def _UnPack(self, corridorParams):
        if corridorParams is None:
            return
        self.router = corridorParams.Router()
        if self.router is not None:
            self.router = self.router.decode('utf-8')
        self.rounds = corridorParams.Rounds()
        self.maxRelaxation = corridorParams.MaxRelaxation()

    # CorridorParamsT
    def Pack(self, builder):
        if self.router is not None:
            router = builder.CreateString(self.router)
        CorridorParamsStart(builder)
        if self.router is not None:
            CorridorParamsAddRouter(builder, router)
        CorridorParamsAddRounds(builder, self.rounds)
        CorridorParamsAddMaxRelaxation(builder, self.maxRelaxation)
        corridorParams = CorridorParamsEnd(builder)
        return corridorParams
