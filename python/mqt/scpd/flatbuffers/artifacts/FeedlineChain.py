# automatically generated by the FlatBuffers compiler, do not modify

# namespace: artifacts

import flatbuffers
from flatbuffers.compat import import_numpy
from typing import Any
from mqt.scpd.flatbuffers.design.PortRef import PortRef
from typing import Optional
np = import_numpy()

# One feedline chain of the assignment.
#
# The model gives every resonator degree two on the ring: a chord to the
# resonator before it, a chord to the one after it, or an end. An end is a
# launcher, or one of the permitted terminations where the feedline simply
# stops. A chain is a run of resonators joined by chords, and it is what the
# Final stage drives with one feedline: launcher, coupler, coupler, …,
# launcher.
class FeedlineChain(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset: int = 0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = FeedlineChain()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsFeedlineChain(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def FeedlineChainBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x53\x43\x50\x31", size_prefixed=size_prefixed)

    # FeedlineChain
    def Init(self, buf: bytes, pos: int):
        self._tab = flatbuffers.table.Table(buf, pos)

    # The ring nodes the chain runs through, as indices into `ring` and
    # `connections`, in ring order. Every one of them is a resonator.
    # FeedlineChain
    def Nodes(self, j: int):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            a = self._tab.Vector(o)
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, a + flatbuffers.number_types.UOffsetTFlags.py_type(j * 4))
        return 0

    # FeedlineChain
    def NodesAsNumpy(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.GetVectorAsNumpy(flatbuffers.number_types.Uint32Flags, o)
        return 0

    # FeedlineChain
    def NodesLength(self) -> int:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # FeedlineChain
    def NodesIsNone(self) -> bool:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

    # The launcher the chain starts at: the launcher slot its first resonator
    # was given. Absent where the chain starts at a termination.
    # FeedlineChain
    def Start(self) -> Optional[PortRef]:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = o + self._tab.Pos
            obj = PortRef()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # The launcher the chain ends at: the launcher slot its last resonator
    # was given. Absent where the chain ends at a termination.
    # FeedlineChain
    def End(self) -> Optional[PortRef]:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            x = o + self._tab.Pos
            obj = PortRef()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

def FeedlineChainStart(builder: flatbuffers.Builder):
    builder.StartObject(3)

def Start(builder: flatbuffers.Builder):
    FeedlineChainStart(builder)

def FeedlineChainAddNodes(builder: flatbuffers.Builder, nodes: int):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(nodes), 0)

def AddNodes(builder: flatbuffers.Builder, nodes: int):
    FeedlineChainAddNodes(builder, nodes)

def FeedlineChainStartNodesVector(builder, numElems: int) -> int:
    return builder.StartVector(4, numElems, 4)

def StartNodesVector(builder, numElems: int) -> int:
    return FeedlineChainStartNodesVector(builder, numElems)

def FeedlineChainAddStart(builder: flatbuffers.Builder, start: Any):
    builder.PrependStructSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(start), 0)

def AddStart(builder: flatbuffers.Builder, start: Any):
    FeedlineChainAddStart(builder, start)

def FeedlineChainAddEnd(builder: flatbuffers.Builder, end: Any):
    builder.PrependStructSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(end), 0)

def AddEnd(builder: flatbuffers.Builder, end: Any):
    FeedlineChainAddEnd(builder, end)

def FeedlineChainEnd(builder: flatbuffers.Builder) -> int:
    return builder.EndObject()

def End(builder: flatbuffers.Builder) -> int:
    return FeedlineChainEnd(builder)

import mqt.scpd.flatbuffers.design.PortRef
try:
    from typing import List, Optional
except:
    pass

class FeedlineChainT(object):

    # FeedlineChainT
    def __init__(
        self,
        nodes = None,
        start = None,
        end = None,
    ):
        self.nodes = nodes  # type: Optional[List[int]]
        self.start = start  # type: Optional[mqt.scpd.flatbuffers.design.PortRef.PortRefT]
        self.end = end  # type: Optional[mqt.scpd.flatbuffers.design.PortRef.PortRefT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        feedlineChain = FeedlineChain()
        feedlineChain.Init(buf, pos)
        return cls.InitFromObj(feedlineChain)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, feedlineChain):
        x = FeedlineChainT()
        x._UnPack(feedlineChain)
        return x

    # FeedlineChainT
    def _UnPack(self, feedlineChain):
        if feedlineChain is None:
            return
        if not feedlineChain.NodesIsNone():
            if np is None:
                self.nodes = []
                for i in range(feedlineChain.NodesLength()):
                    self.nodes.append(feedlineChain.Nodes(i))
            else:
                self.nodes = feedlineChain.NodesAsNumpy()
        if feedlineChain.Start() is not None:
            self.start = mqt.scpd.flatbuffers.design.PortRef.PortRefT.InitFromObj(feedlineChain.Start())
        if feedlineChain.End() is not None:
            self.end = mqt.scpd.flatbuffers.design.PortRef.PortRefT.InitFromObj(feedlineChain.End())

    # FeedlineChainT
    def Pack(self, builder):
        if self.nodes is not None:
            if np is not None and type(self.nodes) is np.ndarray:
                nodes = builder.CreateNumpyVector(self.nodes)
            else:
                FeedlineChainStartNodesVector(builder, len(self.nodes))
                for i in reversed(range(len(self.nodes))):
                    builder.PrependUint32(self.nodes[i])
                nodes = builder.EndVector()
        FeedlineChainStart(builder)
        if self.nodes is not None:
            FeedlineChainAddNodes(builder, nodes)
        if self.start is not None:
            start = self.start.Pack(builder)
            FeedlineChainAddStart(builder, start)
        if self.end is not None:
            end = self.end.Pack(builder)
            FeedlineChainAddEnd(builder, end)
        feedlineChain = FeedlineChainEnd(builder)
        return feedlineChain
