# automatically generated by the FlatBuffers compiler, do not modify

# namespace: artifacts

import flatbuffers
from flatbuffers.compat import import_numpy
from typing import Any
from mqt.scpd.flatbuffers.geometry.Point import Point
from typing import Optional
np = import_numpy()

# Where two partitions meet, and how many wires may cross there.
class PartitionBorder(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset: int = 0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = PartitionBorder()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsPartitionBorder(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def PartitionBorderBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x53\x43\x50\x31", size_prefixed=size_prefixed)

    # PartitionBorder
    def Init(self, buf: bytes, pos: int):
        self._tab = flatbuffers.table.Table(buf, pos)

    # PartitionBorder
    def First(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # PartitionBorder
    def Second(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

    # The cell edges the border runs along, in layout units.
    # PartitionBorder
    def Samples(self, j: int) -> Optional[Point]:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 16
            obj = Point()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # PartitionBorder
    def SamplesLength(self) -> int:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # PartitionBorder
    def SamplesIsNone(self) -> bool:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        return o == 0

    # PartitionBorder
    def Center(self) -> Optional[Point]:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            x = o + self._tab.Pos
            obj = Point()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # PartitionBorder
    def Budget(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(12))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint32Flags, o + self._tab.Pos)
        return 0

def PartitionBorderStart(builder: flatbuffers.Builder):
    builder.StartObject(5)

def Start(builder: flatbuffers.Builder):
    PartitionBorderStart(builder)

def PartitionBorderAddFirst(builder: flatbuffers.Builder, first: int):
    builder.PrependUint32Slot(0, first, 0)

def AddFirst(builder: flatbuffers.Builder, first: int):
    PartitionBorderAddFirst(builder, first)

def PartitionBorderAddSecond(builder: flatbuffers.Builder, second: int):
    builder.PrependUint32Slot(1, second, 0)

def AddSecond(builder: flatbuffers.Builder, second: int):
    PartitionBorderAddSecond(builder, second)

def PartitionBorderAddSamples(builder: flatbuffers.Builder, samples: int):
    builder.PrependUOffsetTRelativeSlot(2, flatbuffers.number_types.UOffsetTFlags.py_type(samples), 0)

def AddSamples(builder: flatbuffers.Builder, samples: int):
    PartitionBorderAddSamples(builder, samples)

def PartitionBorderStartSamplesVector(builder, numElems: int) -> int:
    return builder.StartVector(16, numElems, 8)

def StartSamplesVector(builder, numElems: int) -> int:
    return PartitionBorderStartSamplesVector(builder, numElems)

def PartitionBorderAddCenter(builder: flatbuffers.Builder, center: Any):
    builder.PrependStructSlot(3, flatbuffers.number_types.UOffsetTFlags.py_type(center), 0)

def AddCenter(builder: flatbuffers.Builder, center: Any):
    PartitionBorderAddCenter(builder, center)

def PartitionBorderAddBudget(builder: flatbuffers.Builder, budget: int):
    builder.PrependUint32Slot(4, budget, 0)

def AddBudget(builder: flatbuffers.Builder, budget: int):
    PartitionBorderAddBudget(builder, budget)

def PartitionBorderEnd(builder: flatbuffers.Builder) -> int:
    return builder.EndObject()

def End(builder: flatbuffers.Builder) -> int:
    return PartitionBorderEnd(builder)

import mqt.scpd.flatbuffers.geometry.Point
try:
    from typing import List, Optional
except:
    pass

class PartitionBorderT(object):

    # PartitionBorderT
    def __init__(
        self,
        first = 0,
        second = 0,
        samples = None,
        center = None,
        budget = 0,
    ):
        self.first = first  # type: int
        self.second = second  # type: int
        self.samples = samples  # type: Optional[List[mqt.scpd.flatbuffers.geometry.Point.PointT]]
        self.center = center  # type: Optional[mqt.scpd.flatbuffers.geometry.Point.PointT]
        self.budget = budget  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        partitionBorder = PartitionBorder()
        partitionBorder.Init(buf, pos)
        return cls.InitFromObj(partitionBorder)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, partitionBorder):
        x = PartitionBorderT()
        x._UnPack(partitionBorder)
        return x

    # PartitionBorderT
    def _UnPack(self, partitionBorder):
        if partitionBorder is None:
            return
        self.first = partitionBorder.First()
        self.second = partitionBorder.Second()
        if not partitionBorder.SamplesIsNone():
            self.samples = []
            for i in range(partitionBorder.SamplesLength()):
                if partitionBorder.Samples(i) is None:
                    self.samples.append(None)
                else:
                    point_ = mqt.scpd.flatbuffers.geometry.Point.PointT.InitFromObj(partitionBorder.Samples(i))
                    self.samples.append(point_)
        if partitionBorder.Center() is not None:
            self.center = mqt.scpd.flatbuffers.geometry.Point.PointT.InitFromObj(partitionBorder.Center())
        self.budget = partitionBorder.Budget()

    # PartitionBorderT
    def Pack(self, builder):
        if self.samples is not None:
            PartitionBorderStartSamplesVector(builder, len(self.samples))
            for i in reversed(range(len(self.samples))):
                self.samples[i].Pack(builder)
            samples = builder.EndVector()
        PartitionBorderStart(builder)
        PartitionBorderAddFirst(builder, self.first)
        PartitionBorderAddSecond(builder, self.second)
        if self.samples is not None:
            PartitionBorderAddSamples(builder, samples)
        if self.center is not None:
            center = self.center.Pack(builder)
            PartitionBorderAddCenter(builder, center)
        PartitionBorderAddBudget(builder, self.budget)
        partitionBorder = PartitionBorderEnd(builder)
        return partitionBorder
