# automatically generated by the FlatBuffers compiler, do not modify

# namespace: artifacts

import flatbuffers
from flatbuffers.compat import import_numpy
from typing import Any
from mqt.scpd.flatbuffers.geometry.DCoord import DCoord
from typing import Optional
np = import_numpy()

# One wire's path on the detail grid, cell by cell.
#
# The cells are eight-connected and run from the point the wire is fed at to
# the cell its target port is reached at. A wire the stage could not draw
# carries no cells, so a reader counts the failures rather than being handed
# a separate list that can disagree with the paths beside it.
class DetailWire(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset: int = 0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = DetailWire()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsDetailWire(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def DetailWireBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x53\x43\x50\x31", size_prefixed=size_prefixed)

    # DetailWire
    def Init(self, buf: bytes, pos: int):
        self._tab = flatbuffers.table.Table(buf, pos)

    # DetailWire
    def Path(self, j: int) -> Optional[DCoord]:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = self._tab.Vector(o)
            x += flatbuffers.number_types.UOffsetTFlags.py_type(j) * 8
            obj = DCoord()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # DetailWire
    def PathLength(self) -> int:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            return self._tab.VectorLen(o)
        return 0

    # DetailWire
    def PathIsNone(self) -> bool:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        return o == 0

def DetailWireStart(builder: flatbuffers.Builder):
    builder.StartObject(1)

def Start(builder: flatbuffers.Builder):
    DetailWireStart(builder)

def DetailWireAddPath(builder: flatbuffers.Builder, path: int):
    builder.PrependUOffsetTRelativeSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(path), 0)

def AddPath(builder: flatbuffers.Builder, path: int):
    DetailWireAddPath(builder, path)

def DetailWireStartPathVector(builder, numElems: int) -> int:
    return builder.StartVector(8, numElems, 4)

def StartPathVector(builder, numElems: int) -> int:
    return DetailWireStartPathVector(builder, numElems)

def DetailWireEnd(builder: flatbuffers.Builder) -> int:
    return builder.EndObject()

def End(builder: flatbuffers.Builder) -> int:
    return DetailWireEnd(builder)

import mqt.scpd.flatbuffers.geometry.DCoord
try:
    from typing import List
except:
    pass

class DetailWireT(object):

    # DetailWireT
    def __init__(
        self,
        path = None,
    ):
        self.path = path  # type: Optional[List[mqt.scpd.flatbuffers.geometry.DCoord.DCoordT]]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        detailWire = DetailWire()
        detailWire.Init(buf, pos)
        return cls.InitFromObj(detailWire)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, detailWire):
        x = DetailWireT()
        x._UnPack(detailWire)
        return x

    # DetailWireT
    def _UnPack(self, detailWire):
        if detailWire is None:
            return
        if not detailWire.PathIsNone():
            self.path = []
            for i in range(detailWire.PathLength()):
                if detailWire.Path(i) is None:
                    self.path.append(None)
                else:
                    dCoord_ = mqt.scpd.flatbuffers.geometry.DCoord.DCoordT.InitFromObj(detailWire.Path(i))
                    self.path.append(dCoord_)

    # DetailWireT
    def Pack(self, builder):
        if self.path is not None:
            DetailWireStartPathVector(builder, len(self.path))
            for i in reversed(range(len(self.path))):
                self.path[i].Pack(builder)
            path = builder.EndVector()
        DetailWireStart(builder)
        if self.path is not None:
            DetailWireAddPath(builder, path)
        detailWire = DetailWireEnd(builder)
        return detailWire
