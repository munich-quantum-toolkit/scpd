# automatically generated by the FlatBuffers compiler, do not modify

# namespace: artifacts

import flatbuffers
from flatbuffers.compat import import_numpy
from typing import Any
from mqt.scpd.flatbuffers.design.PortRef import PortRef
from mqt.scpd.flatbuffers.geometry.Point import Point
from typing import Optional
np = import_numpy()

# A launcher and the point at which its wires enter the chip.
class LauncherSlot(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset: int = 0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = LauncherSlot()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsLauncherSlot(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    @classmethod
    def LauncherSlotBufferHasIdentifier(cls, buf, offset, size_prefixed=False):
        return flatbuffers.util.BufferHasIdentifier(buf, offset, b"\x53\x43\x50\x31", size_prefixed=size_prefixed)

    # LauncherSlot
    def Init(self, buf: bytes, pos: int):
        self._tab = flatbuffers.table.Table(buf, pos)

    # LauncherSlot
    def Port(self) -> Optional[PortRef]:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = o + self._tab.Pos
            obj = PortRef()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # LauncherSlot
    def Position(self) -> Optional[Point]:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = o + self._tab.Pos
            obj = Point()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

def LauncherSlotStart(builder: flatbuffers.Builder):
    builder.StartObject(2)

def Start(builder: flatbuffers.Builder):
    LauncherSlotStart(builder)

def LauncherSlotAddPort(builder: flatbuffers.Builder, port: Any):
    builder.PrependStructSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(port), 0)

def AddPort(builder: flatbuffers.Builder, port: Any):
    LauncherSlotAddPort(builder, port)

def LauncherSlotAddPosition(builder: flatbuffers.Builder, position: Any):
    builder.PrependStructSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(position), 0)

def AddPosition(builder: flatbuffers.Builder, position: Any):
    LauncherSlotAddPosition(builder, position)

def LauncherSlotEnd(builder: flatbuffers.Builder) -> int:
    return builder.EndObject()

def End(builder: flatbuffers.Builder) -> int:
    return LauncherSlotEnd(builder)

import mqt.scpd.flatbuffers.design.PortRef
import mqt.scpd.flatbuffers.geometry.Point
try:
    from typing import Optional
except:
    pass

class LauncherSlotT(object):

    # LauncherSlotT
    def __init__(
        self,
        port = None,
        position = None,
    ):
        self.port = port  # type: Optional[mqt.scpd.flatbuffers.design.PortRef.PortRefT]
        self.position = position  # type: Optional[mqt.scpd.flatbuffers.geometry.Point.PointT]

    @classmethod
    def InitFromBuf(cls, buf, pos):
        launcherSlot = LauncherSlot()
        launcherSlot.Init(buf, pos)
        return cls.InitFromObj(launcherSlot)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, launcherSlot):
        x = LauncherSlotT()
        x._UnPack(launcherSlot)
        return x

    # LauncherSlotT
    def _UnPack(self, launcherSlot):
        if launcherSlot is None:
            return
        if launcherSlot.Port() is not None:
            self.port = mqt.scpd.flatbuffers.design.PortRef.PortRefT.InitFromObj(launcherSlot.Port())
        if launcherSlot.Position() is not None:
            self.position = mqt.scpd.flatbuffers.geometry.Point.PointT.InitFromObj(launcherSlot.Position())

    # LauncherSlotT
    def Pack(self, builder):
        LauncherSlotStart(builder)
        if self.port is not None:
            port = self.port.Pack(builder)
            LauncherSlotAddPort(builder, port)
        if self.position is not None:
            position = self.position.Pack(builder)
            LauncherSlotAddPosition(builder, position)
        launcherSlot = LauncherSlotEnd(builder)
        return launcherSlot
