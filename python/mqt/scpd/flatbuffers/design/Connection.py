# automatically generated by the FlatBuffers compiler, do not modify

# namespace: design

import flatbuffers
from flatbuffers.compat import import_numpy
from typing import Any
from mqt.scpd.flatbuffers.design.PortRef import PortRef
from typing import Optional
np = import_numpy()

# One endpoint pair of the solved design.
class Connection(object):
    __slots__ = ['_tab']

    @classmethod
    def GetRootAs(cls, buf, offset: int = 0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, offset)
        x = Connection()
        x.Init(buf, n + offset)
        return x

    @classmethod
    def GetRootAsConnection(cls, buf, offset=0):
        """This method is deprecated. Please switch to GetRootAs."""
        return cls.GetRootAs(buf, offset)
    # Connection
    def Init(self, buf: bytes, pos: int):
        self._tab = flatbuffers.table.Table(buf, pos)

    # Absent for a resonator until the Final stage inserts the coupler that
    # carries the ResonatorSource role.
    # Connection
    def Source(self) -> Optional[PortRef]:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(4))
        if o != 0:
            x = o + self._tab.Pos
            obj = PortRef()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Connection
    def Target(self) -> Optional[PortRef]:
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(6))
        if o != 0:
            x = o + self._tab.Pos
            obj = PortRef()
            obj.Init(self._tab.Bytes, x)
            return obj
        return None

    # Connection
    def SourceRole(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(8))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

    # Connection
    def TargetRole(self):
        o = flatbuffers.number_types.UOffsetTFlags.py_type(self._tab.Offset(10))
        if o != 0:
            return self._tab.Get(flatbuffers.number_types.Uint8Flags, o + self._tab.Pos)
        return 0

def ConnectionStart(builder: flatbuffers.Builder):
    builder.StartObject(4)

def Start(builder: flatbuffers.Builder):
    ConnectionStart(builder)

def ConnectionAddSource(builder: flatbuffers.Builder, source: Any):
    builder.PrependStructSlot(0, flatbuffers.number_types.UOffsetTFlags.py_type(source), 0)

def AddSource(builder: flatbuffers.Builder, source: Any):
    ConnectionAddSource(builder, source)

def ConnectionAddTarget(builder: flatbuffers.Builder, target: Any):
    builder.PrependStructSlot(1, flatbuffers.number_types.UOffsetTFlags.py_type(target), 0)

def AddTarget(builder: flatbuffers.Builder, target: Any):
    ConnectionAddTarget(builder, target)

def ConnectionAddSourceRole(builder: flatbuffers.Builder, sourceRole: int):
    builder.PrependUint8Slot(2, sourceRole, 0)

def AddSourceRole(builder: flatbuffers.Builder, sourceRole: int):
    ConnectionAddSourceRole(builder, sourceRole)

def ConnectionAddTargetRole(builder: flatbuffers.Builder, targetRole: int):
    builder.PrependUint8Slot(3, targetRole, 0)

def AddTargetRole(builder: flatbuffers.Builder, targetRole: int):
    ConnectionAddTargetRole(builder, targetRole)

def ConnectionEnd(builder: flatbuffers.Builder) -> int:
    return builder.EndObject()

def End(builder: flatbuffers.Builder) -> int:
    return ConnectionEnd(builder)

import mqt.scpd.flatbuffers.design.PortRef
try:
    from typing import Optional
except:
    pass

class ConnectionT(object):

    # ConnectionT
    def __init__(
        self,
        source = None,
        target = None,
        sourceRole = 0,
        targetRole = 0,
    ):
        self.source = source  # type: Optional[mqt.scpd.flatbuffers.design.PortRef.PortRefT]
        self.target = target  # type: Optional[mqt.scpd.flatbuffers.design.PortRef.PortRefT]
        self.sourceRole = sourceRole  # type: int
        self.targetRole = targetRole  # type: int

    @classmethod
    def InitFromBuf(cls, buf, pos):
        connection = Connection()
        connection.Init(buf, pos)
        return cls.InitFromObj(connection)

    @classmethod
    def InitFromPackedBuf(cls, buf, pos=0):
        n = flatbuffers.encode.Get(flatbuffers.packer.uoffset, buf, pos)
        return cls.InitFromBuf(buf, pos+n)

    @classmethod
    def InitFromObj(cls, connection):
        x = ConnectionT()
        x._UnPack(connection)
        return x

    # ConnectionT
    def _UnPack(self, connection):
        if connection is None:
            return
        if connection.Source() is not None:
            self.source = mqt.scpd.flatbuffers.design.PortRef.PortRefT.InitFromObj(connection.Source())
        if connection.Target() is not None:
            self.target = mqt.scpd.flatbuffers.design.PortRef.PortRefT.InitFromObj(connection.Target())
        self.sourceRole = connection.SourceRole()
        self.targetRole = connection.TargetRole()

    # ConnectionT
    def Pack(self, builder):
        ConnectionStart(builder)
        if self.source is not None:
            source = self.source.Pack(builder)
            ConnectionAddSource(builder, source)
        if self.target is not None:
            target = self.target.Pack(builder)
            ConnectionAddTarget(builder, target)
        ConnectionAddSourceRole(builder, self.sourceRole)
        ConnectionAddTargetRole(builder, self.targetRole)
        connection = ConnectionEnd(builder)
        return connection
